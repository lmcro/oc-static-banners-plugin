# ===================================
#  Form Field Definitions
# ===================================

fields:
    name:
        label: shohabbos.banner::lang.fields.name
        comment: shohabbos.banner::lang.fields.size_name_comment
        commentHtml: true
        span: left

    image:
        label: shohabbos.banner::lang.fields.image
        span: right
        type: fileupload
        mode: image
        thumbOptions:
            mode: crop
            extension: auto
